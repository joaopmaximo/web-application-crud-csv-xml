spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/main
    username: root
    password: senha01

  sql:
    init:
      mode: always

  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true